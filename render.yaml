services:
  - type: web
    name: code-connect-live
    runtime: docker
    plan: free
    dockerfilePath: Dockerfile
    autoDeploy: true
    region: oregon
    healthCheckPath: /health
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: "80"
